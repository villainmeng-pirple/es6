/* **
 * User's data
** */

/* eslint-disable no-unused-vars */
function UserData(id, email, password, firstName, lastName) {
  this.id = id;
  this.email = email;
  this.password = password;
  this.firstName = firstName;
  this.lastName = lastName;
  this.lists = [];
}
